name: Build Audio HAL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # <-- 添加这一行
    inputs:
      device_codename:
        description: 'Device Codename (e.g., Mi439)'
        required: true
        default: 'Mi439'
